name: cli_bridge
description: 通用 CLI Bridge 框架 — TCP JSON-line 协议中间件,自动参数校验 + 接口文档生成
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

# 纯 Dart 包,不依赖 Flutter SDK
# 用到的仅是 dart:io / dart:convert / dart:async,均为 SDK 自带
