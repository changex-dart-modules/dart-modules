name: sqlite_migrator
description: SQLite 数据库版本迁移管理器 — 自动版本追踪 + 备份 + 回调链
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

# 纯 Dart 包,不依赖 Flutter SDK
dependencies:
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.3.4
  path: ^1.9.1
